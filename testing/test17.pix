fun AverageOfTwo(x: int, y: int) -> float {
    let t0: int = x + y;
    if (t0 > 10) {
        return 0.0;
    }
    let t1: float = t0 / 2 as float;
    return t1;
}

let c1: color = __read 10, 10;
